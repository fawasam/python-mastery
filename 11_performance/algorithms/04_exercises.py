"""
Exercises: Algorithmic Optimization Problems.
"""


def two_sum_optimal(nums: list[int], target: int) -> tuple[int, int] | None:
    """
    Find indices of the two numbers such that they add up to target.
    
    Requirements:
    - Must run in O(n) time and O(n) space using a hash map (dictionary).
    - Do NOT use a nested loop (O(n^2)).
    
    Level 2 - Medium
    """
    raise NotImplementedError("Implement optimal two-sum using dict lookup")


def remove_duplicates_preserve_order(items: list[int]) -> list[int]:
    """
    Remove duplicates from items while preserving original relative order.
    
    Requirements:
    - Must run in O(n) time.
    - Hint: dict.fromkeys() or set tracking.
    
    Level 1 - Easy
    """
    raise NotImplementedError("Implement duplicate removal in O(n) preserving order")
