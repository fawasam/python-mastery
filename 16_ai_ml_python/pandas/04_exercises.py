"""
Exercises: Pandas Feature Split.
"""

import pandas as pd


def split_features_target(df: pd.DataFrame, target_name: str) -> tuple[pd.DataFrame, pd.Series]:
    """
    Exercise: Return tuple (X, y) where X is all columns except target_name, and y is target_name.
    
    Level 1 - Easy
    """
    raise NotImplementedError("Implement split_features_target")
