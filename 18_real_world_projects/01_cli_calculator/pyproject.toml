[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cli-calculator"
version = "1.0.0"
description = "Production CLI Calculator"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0"
]
