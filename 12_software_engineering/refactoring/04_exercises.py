"""
Exercises: Code Refactoring Challenges.
"""


def dirty_discount_calculator(user_type: str, total: float) -> float:
    """
    Refactor using guard clauses and clean math:
    
    if user_type == "GUEST":
        return total
    elif user_type == "MEMBER":
        return total * 0.95
    elif user_type == "PREMIUM":
        return total * 0.85
    else:
        return total
        
    Level 1 - Easy
    """
    raise NotImplementedError("Refactor dirty discount calculator")
